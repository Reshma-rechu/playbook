---
-
  hosts: localhost
  tasks:
    - name: "Ansible create file if it doesn't exist example"
      file:
        path: "/home/ubuntu/{{ ansible_hostname }}.txt"
        state: touch
      
    - name: Adding content to EOF
      lineinfile:
        dest: "/home/ubuntu/reshma/{{ ansible_hostname }}.txt"
        line: "Log file!!"
        insertafter: EOF
